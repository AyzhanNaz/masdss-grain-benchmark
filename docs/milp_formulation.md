# Centralized MILP formulation

TODO: paste the full joint matching–routing–storage mixed-integer program from
Section 3 of the paper (sets, parameters, decision variables, objective Eq. (11),
and all constraints), so that the model in `src/baselines/milp/model.py` can be
verified line-by-line against the mathematics.
