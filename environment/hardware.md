# Hardware and software environment

All reported wall-clock times (Tables 7, 10; Figure 5) were measured on:

- CPU: [model, cores/threads, base clock]
- RAM: [size]
- OS: [name and version]
- Python: [version]; key packages pinned in requirements.txt
- MILP solver: [name, version, license type], threads = [N]

No GPU is used. Runs were executed [sequentially / with N parallel workers];
timing methodology: [wall clock via time.perf_counter, warm-up excluded, ...].
